# Make npm installs more reliable on slow/unstable networks
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000

# Reduce noisy output
progress=false

# Avoid extra registry calls during install
fund=false
audit=false
